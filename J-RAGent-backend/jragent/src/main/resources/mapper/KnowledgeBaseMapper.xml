<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jragent.mapper.KnowledgeBaseMapper">

    <resultMap id="BaseResultMap" type="org.jragent.model.entity.KnowledgeBase">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="metadata" property="metadata"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, description, metadata, created_at, updated_at
    </sql>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
            id,
            name,
            description,
            metadata::text AS metadata,
            created_at,
            updated_at
        FROM knowledge_base
        WHERE id = CAST(#{id} AS uuid)
    </select>

    <select id="selectByIdBatch" resultMap="BaseResultMap">
        SELECT
        id,
        name,
        description,
        metadata::text AS metadata,
        created_at,
        updated_at
        FROM knowledge_base
        WHERE id IN
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            CAST(#{id} AS uuid)
        </foreach>
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
            id,
            name,
            description,
            metadata::text AS metadata,
            created_at,
            updated_at
        FROM knowledge_base
        ORDER BY updated_at DESC
    </select>

    <insert id="insert" parameterType="org.jragent.model.entity.KnowledgeBase"
            keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO knowledge_base
        (
            name,
            description,
            metadata,
            created_at,
            updated_at
        )
        VALUES
            (
                #{name},
                #{description},
                CAST(#{metadata} AS jsonb),
                #{createdAt},
                #{updatedAt}
            )
    </insert>

    <update id="updateById" parameterType="org.jragent.model.entity.KnowledgeBase">
        UPDATE knowledge_base
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="metadata != null">
                metadata = CAST(#{metadata} AS jsonb),
            </if>
            updated_at = NOW()
        </set>
        WHERE id = CAST(#{id} AS uuid)
    </update>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM knowledge_base WHERE id = CAST(#{id} AS uuid)
    </delete>
</mapper>
