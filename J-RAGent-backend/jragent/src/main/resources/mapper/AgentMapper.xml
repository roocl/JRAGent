<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jragent.mapper.AgentMapper">

    <resultMap id="BaseResultMap" type="org.jragent.model.entity.Agent">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="system_prompt" property="systemPrompt"/>
        <result column="model" property="model"/>
        <result column="allowed_tools" property="allowedTools"/>
        <result column="chat_options" property="chatOptions"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, description, system_prompt, model, allowed_tools, chat_options, created_at, updated_at
    </sql>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT id,
               name,
               description,
               system_prompt,
               model,
               allowed_tools::text AS allowed_tools,
                chat_options::text  AS chat_options,
                created_at,
                updated_at
        FROM agent
        WHERE id = CAST(#{id} AS uuid)
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id,
               name,
               description,
               system_prompt,
               model,
               allowed_tools::text AS allowed_tools,
                chat_options::text  AS chat_options,
                created_at,
                updated_at
        FROM agent
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="org.jragent.model.entity.Agent"
            keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO agent
        (name,
         description,
         system_prompt,
         model,
         allowed_tools,
         chat_options,
         created_at,
         updated_at)
        VALUES (#{name},
                #{description},
                #{systemPrompt},
                #{model},
                CAST(#{allowedTools} AS jsonb),
                CAST(#{chatOptions} AS jsonb),
                #{createdAt},
                #{updatedAt})
    </insert>

    <update id="updateById" parameterType="org.jragent.model.entity.Agent">
        UPDATE agent
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="systemPrompt != null">
                system_prompt = #{systemPrompt},
            </if>
            <if test="model != null">
                model = #{model},
            </if>

            <if test="allowedTools != null">
                allowed_tools = CAST(#{allowedTools} AS jsonb),
            </if>

            <if test="chatOptions != null">
                chat_options = CAST(#{chatOptions} AS jsonb),
            </if>
            updated_at = NOW()
        </set>

        WHERE id = CAST(#{id} AS uuid)
    </update>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE
        FROM agent
        WHERE id = CAST(#{id} AS uuid)
    </delete>

</mapper>
