<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jragent.mapper.ChunkBgeM3Mapper">

    <resultMap id="BaseResultMap" type="org.jragent.model.entity.ChunkBgeM3">
        <id column="id" property="id"/>
        <result column="kb_id" property="kbId"/>
        <result column="doc_id" property="docId"/>
        <result column="content" property="content"/>
        <result column="metadata" property="metadata"/>
        <result property="embedding" column="embedding" jdbcType="OTHER"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, kb_id, doc_id, content, metadata, embedding, created_at, updated_at
    </sql>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT id,
               kb_id,
               doc_id,
               content,
               metadata,
               embedding,
               created_at,
               updated_at
        FROM chunk_bge_m3
        WHERE id = CAST(#{id} AS uuid)
    </select>

    <insert id="insert" parameterType="org.jragent.model.entity.ChunkBgeM3"
            keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO chunk_bge_m3
        (kb_id,
         doc_id,
         content,
         metadata,
         embedding,
         created_at,
         updated_at)
        VALUES (CAST(#{kbId} AS uuid),
                CAST(#{docId} AS uuid),
                #{content},
                #{metadata},
                #{embedding}::vector,
                #{createdAt},
                #{updatedAt})
    </insert>

    <update id="updateById" parameterType="org.jragent.model.entity.ChunkBgeM3">
        UPDATE chunk_bge_m3
        <set>
            <if test="kbId != null">
                kb_id = CAST(#{kbId} AS uuid),
            </if>
            <if test="docId != null">
                doc_id = CAST(#{docId} AS uuid),
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="metadata != null">
                metadata = #{metadata},
            </if>
            <if test="embedding != null">
                embedding = #{embedding}::vector,
            </if>
            updated_at = NOW()
        </set>
        WHERE id = CAST(#{id} AS uuid)
    </update>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE
        FROM chunk_bge_m3
        WHERE id = CAST(#{id} AS uuid)
    </delete>

    <select id="similaritySearch" resultMap="BaseResultMap">
        <!-- 不转义xml格式 -->
        <![CDATA[
        SELECT id,
               kb_id,
               doc_id,
               content,
               metadata,
               embedding,
               created_at,
               updated_at
        FROM chunk_bge_m3
        WHERE kb_id = CAST(#{kbId} AS uuid)
        /*根据L2距离排序*/
        ORDER BY embedding <-> #{vectorLiteral}::vector
        LIMIT #{limit}
        ]]>
    </select>
</mapper>
