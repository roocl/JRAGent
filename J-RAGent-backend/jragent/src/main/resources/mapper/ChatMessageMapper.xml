<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jragent.mapper.ChatMessageMapper">

    <resultMap id="BaseResultMap" type="org.jragent.model.entity.ChatMessage">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="metadata" property="metadata"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, session_id, role, content, metadata, created_at, updated_at
    </sql>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
            id,
            session_id,
            role,
            content,
            metadata::text AS metadata,
            created_at,
            updated_at
        FROM chat_message
        WHERE id = CAST(#{id} AS uuid)
    </select>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT
            id,
            session_id,
            role,
            content,
            metadata::text AS metadata,
            created_at,
            updated_at
        FROM chat_message
        WHERE session_id = CAST(#{sessionId} AS uuid)
        ORDER BY created_at
    </select>

    <select id="selectBySessionIdRecently" resultMap="BaseResultMap">
        SELECT
            id,
            session_id,
            role,
            content,
            metadata::text AS metadata,
            created_at,
            updated_at
        FROM chat_message
        WHERE session_id = CAST(#{sessionId} AS uuid)
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <insert id="insert" parameterType="org.jragent.model.entity.ChatMessage"
            keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO chat_message
        (
            session_id,
            role,
            content,
            metadata,
            created_at,
            updated_at
        )
        VALUES
            (
                CAST(#{sessionId} AS uuid),
                #{role},
                #{content},
                CAST(#{metadata} AS jsonb),
                #{createdAt},
                #{updatedAt}
            )
    </insert>

    <update id="updateById" parameterType="org.jragent.model.entity.ChatMessage">
        UPDATE chat_message
        <set>
            <if test="sessionId != null">
                session_id = CAST(#{sessionId} AS uuid),
            </if>
            <if test="role != null">
                role = #{role},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="metadata != null">
                metadata = CAST(#{metadata} AS jsonb),
            </if>
            updated_at = NOW()
        </set>
        WHERE id = CAST(#{id} AS uuid)
    </update>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM chat_message WHERE id = CAST(#{id} AS uuid)
    </delete>

</mapper>
